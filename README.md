# Simple Port Scanner (TCP) 📡

โปรเจกต์พื้นฐานด้าน Network Security ที่สร้างขึ้นโดยใช้ **Python** เพื่อทำการ **Reconnaissance** (การเก็บข้อมูล) โดยการตรวจสอบสถานะของพอร์ต TCP บนเครื่องเป้าหมาย

---

## 📝 จุดประสงค์และหลักการทำงาน

### จุดประสงค์
เครื่องมือนี้ช่วยให้ผู้ใช้สามารถระบุว่าพอร์ตบริการใด ๆ บนโฮสต์เป้าหมายกำลังทำงานอยู่ ซึ่งเป็นขั้นตอนแรกที่สำคัญในการทำ **Penetration Testing** หรือการตรวจสอบความปลอดภัยของเครือข่ายของตนเอง

### หลักการทำงาน
1.  ผู้ใช้ระบุ **IP Address** หรือ **Hostname** และ **ช่วงพอร์ต (Port Range)** ที่ต้องการสแกน
2.  สคริปต์ใช้โมดูลมาตรฐาน **`socket`** ของ Python
3.  สำหรับแต่ละพอร์ตในขอบเขตที่กำหนด สคริปต์จะพยายามทำ **TCP Handshake** (โดยใช้ `connect_ex`)
4.  หากการเชื่อมต่อสำเร็จ (คืนค่าเป็น `0`) จะถือว่า **พอร์ตนั้นเปิดอยู่ (Open)**
5.  มีการตั้งค่า **Timeout** เพื่อให้แน่ใจว่าการสแกนจะไม่ค้างหากพอร์ตถูกบล็อกหรือไม่มีการตอบสนอง

---

## 🚀 การติดตั้งและใช้งาน

### ข้อกำหนดเบื้องต้น

* **Python 3.x**
* ไม่จำเป็นต้องติดตั้งไลบรารีเพิ่มเติม เนื่องจากใช้โมดูลมาตรฐานของ Python (`socket`, `sys`, `datetime`)

### ขั้นตอนการใช้งาน

รันสคริปต์ผ่าน Command Line โดยระบุ **Host/IP** และ **ช่วงพอร์ต** (รูปแบบ `<StartPort>-<EndPort>`):

```bash
python port_scanner.py <IP_ADDRESS_OR_HOST> <START_PORT-END_PORT>
